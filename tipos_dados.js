//var, const e let

//1 - const (constante)
const pi = 3.14;
//imprimindo a variável no console
//console.log(pi);

//2 - let (o valor da variável pode ser alterado)
let nome = 'Angelo'

let nome_atualizado = 'Angelo Lindão!';
console.log(nome);

let desconto = 20.5;

// atulizando o valor da variavel utilizando a própria variável
desconto = desconto * 2;
console.log('Desconto atualizado: ' + desconto);

//Array (lista)
let tamanhos = ['P', 'PP', 'M', 'G', 36, 38, 40, 54];

let temPet = false;

const idade = 28;

let dataCadastro = new Date(); //pega a data e hora atual
console.log(dataCadastro);
